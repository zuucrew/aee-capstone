# URL of the FastAPI backend. Used by vite.config.ts as the /api proxy target.
# In dev: Vite forwards /api/* → $VITE_API_URL/*
# In prod: set this to your deployed API hostname.
VITE_API_URL=http://localhost:8000
